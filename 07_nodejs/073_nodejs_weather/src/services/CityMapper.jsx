////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////

var CityMapper = {
    nameToOpenWeatherMapId(cityName) {
        return cityName;
    },
    openWeatherMapIdToName(cityId) {
        return cityId;
    },
}

module.exports = CityMapper;
